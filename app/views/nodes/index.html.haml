- title "Nodes"

%table
  %tr
    %th Name
  - for node in @nodes
    %tr
      %td= node.name
      %td= link_to 'Show', node
      %td= link_to 'Edit', edit_node_path(node)
      %td= link_to 'Destroy', node, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Node", new_node_path
